[pytest]
# main.py replaces sys.stdout/stderr at module level (for UTF-8 on Windows),
# which conflicts with pytest's default output capture and causes tests that
# import main to fail. Disable capture globally; no existing test relies on it.
addopts = -s
